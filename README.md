# Dev Branch – Integration

The `dev` branch is the integration branch where new features are merged and tested before release.

## Highlights
- Merged changes from `feature`.
- Practiced resolving merge conflicts in `demo.txt`.
- Serves as a staging ground before merging into `main`.
